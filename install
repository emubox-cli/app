#!/bin/bash

# stackoverflow teaching me things i didn't know
# https://stackoverflow.com/a/14909927
trap 'rm -f /tmp/make_emubox' 0
trap 'exit $?' 1 2 3 15

curl -o /tmp/make_emubox https://emubox.wolves-are.gay/make_emubox
chmod +x /tmp/make_emubox
/tmp/make_emubox